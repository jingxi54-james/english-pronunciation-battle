# 🚀 部署准备完成

## 项目已准备好上传到 Netlify

### 📁 最终项目结构

```
.
├── index.html              # 主页面
├── game.js                 # 游戏逻辑
├── vocabulary.js           # 词汇库
├── style.css               # 样式
├── package.json            # 项目配置
├── package-lock.json       # 依赖锁定
├── netlify.toml            # Netlify 配置
├── .gitignore              # Git 忽略配置
└── README.md               # 项目说明
```

### ✅ 已清理的文件

- ✅ 删除了所有临时文档文件
- ✅ 删除了 server.js（Netlify 不需要）
- ✅ 删除了 leaderboard.json（本地备份）
- ✅ 删除了所有测试文件
- ✅ 删除了所有 SQL 脚本

### ✅ 配置已更新

- ✅ package.json - 适配静态网站
- ✅ netlify.toml - 正确的构建配置
- ✅ index.html - 包含 Supabase 库
- ✅ game.js - 使用 Supabase 直接连接

## 部署步骤

### 步骤 1: 推送到 GitHub

```bash
git add .
git commit -m "Deploy: Ready for Netlify"
git push origin main
```

### 步骤 2: 在 Netlify 中部署

1. 访问 https://netlify.com
2. 登录你的账户
3. 点击 "New site from Git"
4. 选择 GitHub 仓库
5. 配置构建设置：
   - Build command: `npm install`
   - Publish directory: `.`
6. 点击 "Deploy site"

### 步骤 3: 等待部署完成

Netlify 会自动：
1. 克隆你的 GitHub 仓库
2. 运行 `npm install`
3. 发布所有文件
4. 分配一个网址

### 步骤 4: 测试部署

1. 打开 Netlify 分配的网址
2. 登录并完成一局游戏
3. 点击"保存并退出"
4. 打开 F12 查看 Console 日志
5. 应该看到 ✅ 成绩已保存到 Supabase

### 步骤 5: 验证数据同步

1. 在浏览器 A 中完成游戏并保存
2. 在浏览器 B 中打开游戏
3. 点击"查看排行榜"
4. 应该看到浏览器 A 中的成绩

## 重要提醒

### 数据库设置

在部署前，确保已在 Supabase 中：

1. ✅ 创建了 `leaderboard` 表
2. ✅ 配置了 RLS 策略（允许所有人读写）
3. ✅ 验证了 API Key 正确

### Supabase 配置

游戏中已配置的 Supabase 凭证：

```javascript
const SUPABASE_URL = 'https://wmaxoenjqvdnwlramwvp.supabase.co';
const SUPABASE_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

## 部署后的 URL

部署完成后，你会获得一个 Netlify 网址，例如：
```
https://your-site-name.netlify.app
```

## 常见问题

### Q: 部署失败怎么办？

A: 检查 Netlify 的构建日志，通常是因为：
- 缺少依赖（运行 `npm install`）
- 代码语法错误
- 环境配置问题

### Q: 数据没有保存怎么办？

A: 检查：
1. Supabase 表是否存在
2. RLS 策略是否正确
3. API Key 是否正确
4. 浏览器 Console 中的错误信息

### Q: 如何自定义网址？

A: 在 Netlify Dashboard 中：
1. 进入 Site settings
2. 点击 "Change site name"
3. 输入新的网址名称

## 完成！

项目已准备好部署到 Netlify。

推送代码到 GitHub，Netlify 会自动部署！
